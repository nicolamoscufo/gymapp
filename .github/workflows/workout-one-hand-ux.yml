name: Workout One Hand UX

on:
  push:
    branches:
      - feature/workout-one-hand-ux

permissions:
  contents: write

jobs:
  patch-and-test:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/workout-one-hand-ux

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Apply one-hand UX patch
        run: |
          python3 .github/scripts/workout_one_hand_ux_patch.py
          python3 .github/scripts/workout_one_hand_test_fix.py

      - name: Format
        run: dart format lib/screens/active_workout.dart test/workout_ux_polish_v2_test.dart

      - name: Install dependencies
        run: flutter pub get

      - name: Run focused tests
        run: flutter test test/workout_ux_polish_v2_test.dart test/active_workout_screen_test.dart

      - name: Analyze
        run: dart analyze

      - name: Commit generated patch
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/screens/active_workout.dart test/workout_ux_polish_v2_test.dart
          git commit -m "Improve one-hand workout UX"
          git push origin HEAD:feature/workout-one-hand-ux
